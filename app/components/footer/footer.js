'use strict';

import React, {Component} from 'react';
import styles from './styles.css';

class Footer extends Component {
  render() {
    return (
      <footer className={ styles.root }>text</footer>
    );
  }
}

export default Footer
